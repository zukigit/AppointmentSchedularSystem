import { RegisterationRequestModel } from './registeration-request-model';

describe('RegisterationRequestModel', () => {
  it('should create an instance', () => {
    expect(new RegisterationRequestModel()).toBeTruthy();
  });
});
